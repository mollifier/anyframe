local peco_path
zstyle -s ":anyframe:selector:peco:" command peco_path || peco_path='peco'

if [[ -n "$1" ]]; then
  ${=peco_path} --query "$1"
else
  ${=peco_path}
fi

# Local Variables:
# mode: Shell-Script
# End:
# vim: ft=zsh
