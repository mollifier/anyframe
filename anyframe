local source="$1"
local selector="$2"
local action="$3"

local selected_item="$($source | $selector "$LBUFFER")"
$action "$selected_item"
if zle; then
  zle clear-screen
fi

# Local Variables:
# mode: Shell-Script
# End:
# vim: ft=zsh
