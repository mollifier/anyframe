local action="$1"

local selected_item="$(cat)"
if [[ -n "$selected_item" ]]; then
  $action "$selected_item"
  if zle; then
    zle clear-screen
  fi
fi

# Local Variables:
# mode: Shell-Script
# End:
# vim: ft=zsh
