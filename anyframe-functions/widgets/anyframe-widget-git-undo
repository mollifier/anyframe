anyframe-source-git-reflog -n \
  | anyframe-selector-auto \
  | awk -F ':' '{ print "HEAD@{" $1 "}" }' \
  | anyframe-action-execute git reset --hard

# Local Variables:
# mode: Shell-Script
# End:
# vim: ft=zsh
